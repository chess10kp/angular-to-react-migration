{
  "name": "schemas-v2-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic orchestrator skeleton for the framework-neutral migration engine (ORCHESTRATOR.md + TOOL-CONTRACTS.md).",
  "scripts": {
    "test": "node --test 'test/*.test.mjs'"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
